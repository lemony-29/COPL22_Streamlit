[tool.poetry]
name = "streamlit-app"
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10"
streamlit = ">=1.30"
pandas = ">=2.0"
requests = ">=2.31"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"